# HR Policy Agentic RAG — Static Demo

Standalone, client-side demo of the HR Policy Agentic RAG Copilot UI.
No backend, no API keys needed — everything runs in the browser.

Open `index.html` directly, or serve the folder with any static file server.

For the real backend (LangGraph + FastAPI + OpenAI + Pinecone + Tavily),
see the main project repo.
